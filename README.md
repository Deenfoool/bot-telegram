# WinHelper Telegram Bot

**WinHelper** — это Telegram-бот, предназначенный для помощи в настройке, оптимизации, очистке и диагностике проблем с Windows 11. Бот предоставляет инструкции и готовые скрипты для решения распространённых задач.

---

## ✅ Возможности

*   **Настройка Windows:**
    *   Отключение автозапуска программ.
    *   Ограничение или отключение телеметрии.
    *   Отключение поиска Bing и Кортаны в меню "Пуск".
    *   И другие настройки приватности и удобства.
*   **Оптимизация производительности:**
    *   Рекомендации по отключению ненужных служб.
    *   Настройка визуальных эффектов.
    *   Управление файлом подкачки.
    *   И другие советы по улучшению быстродействия.
*   **Очистка системы:**
    *   Инструкции по очистке временных файлов, кэша обновлений, DNS и т.д.
    *   Скрипты для автоматизации очистки.
*   **Готовые скрипты:**
    *   Скрипт для очистки диска.
    *   Скрипт активации Windows (MAS).
    *   Архив с инструментами обхода блокировок (zapret).
*   **Решение ошибок BSOD:**
    *   Пользователь отправляет код ошибки (например, `0x00000001`).
    *   Бот находит и отправляет подробные инструкции по её устранению.
*   **Список кодов ошибок:**
    *   По запросу бот отправляет полный список известных кодов ошибок Windows и их названий.

---

## 🛠️ Установка и запуск

> ⚠️ **Важно:** Работа с системными настройками Windows может повлиять на стабильность и безопасность системы. Всегда создавайте точку восстановления перед внесением изменений.

### Требования

*   Python 3.8 или выше
*   pip (менеджер пакетов Python)

### Установка

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/deenfoool/bot-telegram.git
    cd bot-telegram
    ```

2.  **Создайте и активируйте виртуальное окружение (рекомендуется):**
    *   **Debian/Ubuntu:**
        ```bash
        sudo apt install python3-venv
        python3 -m venv venv
        source venv/bin/activate
        ```
    *   **AlmaLinux/RHEL:**
        ```bash
        sudo dnf install python3-venv
        python3 -m venv venv
        source venv/bin/activate
        ```

3.  **Установите зависимости:**
    ```bash
    pip install -r req.txt
    ```
    > ⚠️ Установка `pyxDamerauLevenshtein` может потребовать компилятор GCC (`sudo apt install build-essential` или `sudo dnf install gcc gcc-c++`).

---

## 🔐 Настройка токена

> ⚠️ **НИКОГДА не храните токен в открытом виде в коде или в репозитории!**

1.  Получите токен у [@BotFather](https://t.me/BotFather) в Telegram.
2.  Установите токен как переменную окружения:
    ```bash
    export BOT_TOKEN="ваш_токен_от_botfather"
    ```

---

## ▶️ Запуск бота

Убедитесь, что виртуальное окружение активировано и переменная `BOT_TOKEN` установлена, затем:

```bash
python main.py
